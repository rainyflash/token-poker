name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  windows:
    name: Windows quality gate
    runs-on: windows-latest
    timeout-minutes: 45

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: npm
          cache-dependency-path: |
            ui/package-lock.json
            plugins/token-holdem/mcp/package-lock.json

      - name: Report pinned toolchains
        shell: pwsh
        run: |
          rustup show active-toolchain
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          rustc --version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node --version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          npm --version

      - name: Check Rust formatting and lints
        shell: pwsh
        run: |
          cargo fmt --all -- --check
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          cargo clippy --workspace --all-targets --locked -- -D warnings

      - name: Run Rust tests
        shell: pwsh
        run: cargo test --workspace --locked

      - name: Build sidecar and fuzz targets
        shell: pwsh
        run: |
          cargo build --locked -p token-holdem-sidecar
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          cargo check --manifest-path fuzz/Cargo.toml --bins --locked

      - name: Build and test UI
        working-directory: ui
        shell: pwsh
        run: |
          npm ci
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          npm run build
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          npm run lint
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          npm test

      - name: Build and test Codex plugin
        working-directory: plugins/token-holdem/mcp
        shell: pwsh
        run: |
          npm ci
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          npm run build
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          npm test

      - name: Validate scripts and source language
        shell: pwsh
        run: |
          ./scripts/check-syntax.ps1
          ./scripts/install-plugin.ps1 -ValidateOnly
          ./scripts/test-installer-entrypoint.ps1
          ./scripts/test-codex-runtime-resolution.ps1
          ./scripts/test-forward-update-contract.ps1
          ./scripts/test-network-suite.ps1
          python -m py_compile scripts/build-plugin.py scripts/check-rust-security-baseline.py scripts/set-version.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python scripts/check-rust-security-baseline.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python scripts/check-source-language.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node --test scripts/community-network.test.mjs

      - name: Build production networking runtime
        shell: pwsh
        run: cargo build --release --locked -p token-holdem-sidecar

      - name: Verify volunteer networking and complete sessions
        shell: pwsh
        env:
          TOKEN_HOLDEM_SIDECAR_PATH: ${{ github.workspace }}\target\release\token-holdem-sidecar.exe
        run: |
          ./scripts/verify-network-suite.ps1
